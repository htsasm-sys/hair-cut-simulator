# HairCut Simulator — Claude Code 引き継ぎブリーフ

ORIGIN'S（美容室）向け「AI画像解析＆骨格・毛流補正付き3Dリアルタイムカット展開図シミュレーター」。
claude.ai のチャットで対話的にプロトタイピングし、このリポジトリに書き出した状態。
ここから先は Claude Code で継続開発する想定。

## 起動方法
```bash
npm install
npm run dev
```
`http://localhost:5173` で開く。エントリは `src/App.jsx`（本体はこの1ファイルに実装済み）。

## これまでの経緯（時系列）

1. **初回要望**：正面写真アップロード→AIによるサイド/バック予測→3D頭部モデルへ反映、
   縦横スライスの双方向クロスチェック、骨格・毛流（つむじ/生え癖）による物理補正、
   手動スライダーでの微調整、LocalStorageプリセット保存、A4印刷/PDF出力までを含む
   フル機能仕様（詳細は下記「原仕様」参照）。
2. **不具合修正①**：展開図パネルが見えない → 髪の長さ(cm)を100で割っていたスケールバグ。
   頭部半径に対する比率に修正済み。
3. **不具合修正②**：「長さが頭の中心からになっている、頭皮からにしてほしい」→
   パネルのアンカーを固定半径1.0の球面ではなく、`headSurfacePoint()` で実際に変形させた
   頭皮表面の座標から計算するように修正。骨格スライダー変更時も追従する。
4. **デザイン刷新**：ダークテーマの3Dビューから「美容師教科書イラスト風」へ変更
   （白背景／白いワイヤーフレーム頭部／ピンクの太枠パネル＋薄ピンク面／
   技法名・ダイレクション指示の日本語テキストラベル／ピンク・ブルーのダイレクション矢印）。
5. **頭部モデルの差し替え**：手続き型の球体ジオメトリから、スキャン済みの頭部メッシュ
   （`public/models/head.glb`）に変更。`GLTFLoader` で読み込み・中心化・スケール調整した上で、
   方向ごとの実測半径をルックアップテーブル化（`buildHeadLookup` / `lookupHeadRadius`）。
   骨格・生え際スライダーはこの実測半径に対して従来と同じ倍率補正をかける形で存続。
   密なメッシュ（約7万三角形）のため、輪郭ワイヤーフレームはスライダー操作のたびに
   再構築せず、モデル読み込み時の1回のみ生成（パフォーマンス対策）。
   モデル出典：Sketchfab「Female_head_retopo」by Redninjamp
   （CC-BY-4.0, https://sketchfab.com/3d-models/female-head-retopo-5003044e1a3c4c38926c85ce00c18399）。
   本番運用時はライセンス表記の要否を確認し、必要なら別モデルへの差し替えを検討。

## この環境（claude.ai）特有の制約で簡略化した箇所

Claude Code / 実際の開発環境では、以下は原仕様どおりフル実装が可能なはずなので、
優先的に置き換えを検討してほしい：

- **AI画像解析・生成**：gpt-4o 等への実API接続はしておらず、`AI_STYLE_TEMPLATES` という
  3パターンの固定プリセット値で代替している（`src/App.jsx` 内）。実際は正面写真を
  Vision系APIに渡してネープ長・重さ・レイヤー感を推定し、初期パラメータを生成する処理に
  差し替える必要がある。
- **3Dライブラリ**：`@react-three/fiber` / `drei` / `OrbitControls` が使えない環境だったため、
  素の `three.js` ＋自前実装のオービットコントロールで組んでいる。Claude Code環境なら
  r3f + drei に載せ替えて保守性を上げるのもあり（ただし現状で機能は一通り動く）。
  現バージョンは `three@^0.155` を想定（package.jsonに記載）。
- **PDF出力**：`jspdf` / `html2canvas` が使えなかったため、`canvas.toDataURL()` による
  マルチアングル自動撮影 → ブラウザの `window.print()`（A4 CSS）で代替している
  （`PrintReport` コンポーネント、`src/App.jsx` 末尾）。
- **プリセット保存**：`localStorage` のみ。ユーザーは Supabase を他プロジェクト
  （HairCraft）で使っているので、スタッフ間共有やスタイル履歴が必要になったら
  Supabase移行を検討。

## 原仕様（要約）

- 技術スタック希望：React(Vite) + TypeScript, three.js/r3f/drei, Tailwind CSS + lucide-react,
  html2canvas/jspdf（TSは未対応、上記の理由でJSXのまま・html2canvas/jspdfは未使用）
- 正面写真アップロード → AI予測3パターン選択 → 初期3Dパラメータ自動反映
- 引き出し展開図⇔自然落下トグル、アイロンサイズ選択、カール補正(100〜140%)
- 縦スライス⇔横スライス(クロスチェック)の双方向切替、頭部曲率に応じたカットライン再構成
- 骨格（ハチ張り/絶壁・丸み/トップ高さ）、生え際（ネープ高さ/おでこ広さ）、
  毛流・生え癖（つむじ位置・回転方向、ネープ/前髪の生え癖）の補正モジュール
- 手動スライダー（引き出し角度0-180°、オーバーダイレクション、スライス傾斜）、
  フリーデザインモード、名前付きプリセット保存
- A4印刷/PDF：5アングル自動撮影＋スペック表＋カルテメモ欄

## 次にやると良さそうなこと（提案・未着手）

- [ ] 実際の画像認識/生成AIとの接続（AI予測パートの本実装）
- [ ] TypeScript化（原仕様の希望に合わせる場合）
- [ ] Supabase連携でプリセット・カルテをクラウド保存（HairCraftと合わせるなら）
- [ ] 毛流物理演算（自然落下時の毛束の揺れ・重なり）の精度向上
- [ ] モバイル実機での操作性（ピンチズーム/パン）の調整

## ファイル構成
```
hair-cut-simulator/
├── index.html
├── package.json
├── vite.config.js
├── tailwind.config.js
├── postcss.config.js
└── src/
    ├── main.jsx
    ├── index.css
    └── App.jsx   ← 本体（シミュレーター全体、約1100行）
```
